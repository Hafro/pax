% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pax_marmap.R
\name{pax_marmap_ocean_depth}
\alias{pax_marmap_ocean_depth}
\title{Fetch and aggregate ocean depth data}
\usage{
pax_marmap_ocean_depth(lat1 = NULL, lon1 = NULL, lat2 = NULL, lon2 = NULL)
}
\arguments{
\item{lat1, lon1}{Minimum latitude and longitude of the bounding box.
When all four bounds are \code{NULL} the bundled default-bounds dataset is
used instead of fetching from NOAA.}

\item{lat2, lon2}{Maximum latitude and longitude of the bounding box}
}
\value{
A data.frame with columns \code{lon}, \code{lat}, \code{ocean_depth}, and
\code{h3_cell}, decorated with \code{pax_name = "ocean_depth"} for use with
\code{\link[=pax_import]{pax_import()}}
}
\description{
Retrieves bathymetric data via the marmap package (or uses the pre-cached
package dataset when no bounds are given) and aggregates it to H3 cells at
the current database resolution.
}
