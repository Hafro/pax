% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pax_si.R
\name{pax_si}
\alias{pax_si}
\alias{pax_si_scale_by_alk}
\alias{pax_si_scale_by_landings}
\alias{pax_si_by_length}
\alias{pax_si_scale_winsorize}
\alias{pax_si_scale_by_strata}
\alias{pax_si_strata_summary}
\alias{pax_si_year_summary}
\title{Survey index computation functions}
\usage{
pax_si_scale_by_alk(
  tbl,
  lgroups = seq(0, 200, 5),
  regions = list(all = 101:115),
  tgroup = NULL,
  ygroup = NULL,
  gear_group = NULL,
  alk_tbl
)

pax_si_scale_by_landings(
  tbl,
  species,
  landings_tbl = dplyr::tbl(dbplyr::remote_con(tbl), "landings"),
  logbook_tbl = dplyr::tbl(dbplyr::remote_con(tbl), "logbook"),
  regions = list(all = 101:115),
  gear_group = list(Other = "Var", BMT = c("BMT", "NPT", "SHT", "PGT"), LLN = "LLN", DSE
    = c("PSE", "DSE")),
  tgroup = list(t1 = 1:6, t2 = 7:12)
)

pax_si_by_length(
  tbl,
  ldist = pax_ldist_add_weight(dplyr::tbl(dbplyr::remote_con(tbl), "ldist"))
)

pax_si_scale_winsorize(tbl, q = 0.95)

pax_si_scale_by_strata(tbl, strata_tbl, area_col = "rall_area")

pax_si_strata_summary(tbl, length_range = c(5, 500), std.cv = 0.2)

pax_si_year_summary(tbl)
}
\arguments{
\item{tbl}{A dplyr query, typically from the station table}

\item{lgroups}{Numeric vector of length group lower bounds}

\item{regions}{Named list mapping region names to vectors of division codes}

\item{tgroup}{Named list mapping temporal group names to vectors of month
integers, or \code{NULL} for a single annual group}

\item{ygroup}{Named list mapping year group names to vectors of year
integers, or \code{NULL} for one group per year}

\item{gear_group}{Named list mapping gear group names to vectors of
\code{mfdb_gear_code} values}

\item{alk_tbl}{A dplyr query with \code{agep} column, as returned by
\code{\link[=pax_ldist_alk]{pax_ldist_alk()}}}

\item{species}{Integer species code}

\item{landings_tbl}{A dplyr query from the landings table}

\item{logbook_tbl}{A dplyr query from the logbook table, used to disaggregate
landings by area when more than one region is defined}

\item{ldist}{A dplyr query from the ldist table, pre-processed with
\code{\link[=pax_ldist_add_weight]{pax_ldist_add_weight()}} to provide a \code{weight} column}

\item{q}{Quantile threshold above which station biomass values are
winsorized (default 0.95)}

\item{strata_tbl}{A dplyr query or table name for strata polygons, with
columns \code{stratum}, \code{h3_cells}, \code{rall_area}, and \code{geom}}

\item{area_col}{Name of the column in \code{strata_tbl} containing the
surveyable area in km² (default \code{"rall_area"})}

\item{length_range}{Numeric vector of length 2, only fish within this length
range (exclusive) contribute to the summary}

\item{std.cv}{Coefficient of variation used as a floor for the standard
deviation when all station values within a stratum are identical}
}
\value{
\subsection{pax_si_scale_by_alk}{A dplyr query with \code{si_abund}
and \code{si_biomass} scaled by the age-length key proportions, filtered to
positive values}

\subsection{pax_si_scale_by_landings}{A dplyr query with
\code{si_abund} and \code{si_biomass} rescaled so that total biomass matches
commercial landings}

\subsection{pax_si_by_length}{A dplyr query joining station and
length data, with \code{si_abund} (thousands of fish per stratum) and
\code{si_biomass} (tonnes per stratum) columns added}

\subsection{pax_si_scale_winsorize}{A dplyr query with extreme
\code{si_abund} and \code{si_biomass} values scaled down to the \code{q}
quantile within each year and species}

\subsection{pax_si_scale_by_strata}{A dplyr query with each station
assigned to a stratum, and \code{si_abund} and \code{si_biomass} multiplied by
the stratum area (in square nautical miles) and divided by the number of
stations in that stratum}

\subsection{pax_si_strata_summary}{A dplyr query aggregated by
species, year, stratum, sampling type, and area, with columns
\code{si_N}, \code{si_abund}, \code{si_abund_sd}, \code{si_biomass}, and
\code{si_biomass_sd}}

\subsection{pax_si_year_summary}{A dplyr query aggregated by
species, sampling type, and year, with columns \code{si_N}, \code{si_abund},
\code{si_abund_cv}, \code{si_biomass}, and \code{si_biomass_cv}}
}
\description{
Functions to compute and scale survey indices of abundance and biomass.
The typical workflow is:
\enumerate{
\item \code{\link[=pax_si_by_length]{pax_si_by_length()}} -- join station and length data, scale by tow area
\item \code{\link[=pax_si_scale_by_strata]{pax_si_scale_by_strata()}} -- multiply by stratum area
\item \code{\link[=pax_si_strata_summary]{pax_si_strata_summary()}} -- aggregate within strata
\item \code{\link[=pax_si_year_summary]{pax_si_year_summary()}} -- aggregate across strata to annual totals
}
}
