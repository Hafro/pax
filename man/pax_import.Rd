% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pax.R
\name{pax_import}
\alias{pax_import}
\title{Import a table into a pax database}
\usage{
pax_import(
  pcon,
  tbl,
  overwrite = FALSE,
  name = attr(tbl, "pax_name"),
  cite = attr(tbl, "pax_cite")
)
}
\arguments{
\item{pcon}{A pax DBI connection, as returned by \code{\link[=pax_connect]{pax_connect()}}}

\item{tbl}{Data to import: a data.frame, sf object, dplyr query, or path
to a CSV file}

\item{overwrite}{Boolean, overwrite an existing table with the same name?}

\item{name}{Name to use for the imported table. Defaults to the variable
name of \code{tbl}, or the \code{pax_name} attribute set by \code{\link[=pax_decorate]{pax_decorate()}}}

\item{cite}{Citation string for the data source. Defaults to the
\code{pax_cite} attribute set by \code{\link[=pax_decorate]{pax_decorate()}}}
}
\value{
Invisibly returns \code{NULL}
}
\description{
Import a data.frame, sf spatial object, or CSV file into a pax DuckDB
database. Geometry and H3 spatial index columns are added automatically
when the data contains a geometry column, \code{lat}/\code{lon} columns, or
\code{begin_lat}/\code{begin_lon}/\code{end_lat}/\code{end_lon} columns.
}
